import { useEffect, useState } from "react";

export default function useFetch(url) {
  const [state, setState] = useState({ folders: null, notes: null });

  useEffect(() => {
    // setState(state => )
  });
}